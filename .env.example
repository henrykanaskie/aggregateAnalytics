# Copy to .env (gitignored). Only the dashboard reads this.
# The Odds API key for multi-sportsbook player props: https://the-odds-api.com
ODDS_API_KEY=
# Optional overrides
# NFL_SEASON=2026
# DASHBOARD_PORT=8017
